PORT=5001
JWT_SECRET=replace_with_a_long_random_secret
# MongoDB Atlas connection string (include a database name in the path, e.g. /food-ordering)
MONGO_URI=mongodb+srv://user:password@cluster0.example.mongodb.net/food-ordering
# Comma-separated list of allowed origins for CORS, e.g. your GitHub Pages URL.
# Leave unset (or "*") to allow all origins.
CORS_ORIGIN=https://harshitmakkar31.github.io
